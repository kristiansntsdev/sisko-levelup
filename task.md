# Tasks

## Blocked

- [ ] Approval member harus sampai core
  - Sekarang: volunteer auto (userlevel 1), squad via tabel `upgrade` + approve admin kota (userlevel 2), lalu row dihapus.
  - Belum: join core, approve admin untuk `member=3`, naikkan `userlevel` ke 3.
  - Blocked by: slice ini stop di squad.
